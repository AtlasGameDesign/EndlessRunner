using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class whitescreen : MonoBehaviour
{


    void Start()
    {

    }

    void Update()
    {
        Destroy(gameObject, 2); //5 is how many seconds you want before the object deletes itself
            }
 }